{
  "name": "calling-stats",
  "private": true,
  "version": "1.0.0",
  "description": "Calling signaling server with prometheus stats",
  "main": "src/stats.js",
  "scripts": {
    "start": "node src/main.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "AGPL-3.0",
  "dependencies": {
    "calling-signaling": "^0.1.3",
    "express": "^4.17.1",
    "prom-client": "^12.0.0"
  },
  "bin": {
    "calling-stats": "src/main.js"
  }
}
